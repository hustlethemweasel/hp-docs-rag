# Baked into the client bundle at build time (Next.js inlines NEXT_PUBLIC_* vars).
# The browser calls this URL directly, so it must be the host-published API port
# whether running via `next dev` or the Compose frontend container.
NEXT_PUBLIC_API_URL=http://localhost:8000
